<h1><%= @user.name %><% if @user.admin? %> [Admin]<% end %><% if @user.blocked? %> [BLOCKED]<% end %></h1>

<p>
  <%= link_to image_tag(@user.image.file.thumb.url , class: 'img-circle'), user_image_path(@user), title: "#{@user.name}" %>
</p>

<div class="follow">
  <%= render partial: 'follow', locals: { user: @user } %>
</div>

<% if @user.following.count > 0 %><h3>Is following:</h3>
  <% @user.following.each do |user| %>
    <%= link_to image_tag(user.image.file.bit.url , class: 'img-circle'), user, title: "#{user.name}" %>
  <% end %>
<% end %>

<%= render partial: 'posts/posts', locals: { posts: @user.posts } %>
